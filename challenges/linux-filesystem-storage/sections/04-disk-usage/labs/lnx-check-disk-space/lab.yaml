id: lnx-check-disk-space
title: "Check filesystem utilization using df"
xp: 15
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "rm -rf $HOME/disk-test"
    - "mkdir -p $HOME/disk-test"
version: 1
