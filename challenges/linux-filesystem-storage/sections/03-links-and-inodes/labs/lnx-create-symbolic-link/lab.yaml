id: lnx-create-symbolic-link
title: Create a symbolic link to a configuration file
xp: 20
setup:
  seed_commands:
  - rm -rf $HOME/link-test
  - mkdir -p $HOME/link-test/configs
  - echo 'PORT=8080' > $HOME/link-test/configs/app_v2.conf
