id: lnx-process-command-output
title: "Process Command Output with Head and Redirection"
xp: 15
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/text-test"
    - "seq 1 20 | sed 's/^/Line /' > $HOME/text-test/raw_logs.txt"
    - "rm -f $HOME/text-test/first_five.txt"
version: 1
