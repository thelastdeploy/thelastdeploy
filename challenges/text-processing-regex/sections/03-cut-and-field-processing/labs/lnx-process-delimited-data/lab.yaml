id: lnx-process-delimited-data
title: "Process Delimited CSV Files with Cut"
xp: 25
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/text-test"
    - "printf 'alice,admin,1001\nbob,user,1002\ncharlie,dev,1003\n' > $HOME/text-test/users.csv"
    - "rm -f $HOME/text-test/user_uids.txt"
version: 1
