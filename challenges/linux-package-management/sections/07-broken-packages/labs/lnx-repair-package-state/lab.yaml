id: lnx-repair-package-state
title: Resolve stale package manager lock files
xp: 40
setup:
  seed_commands:
  - rm -rf $HOME/broken-test
  - mkdir -p $HOME/broken-test/lock_zone
  - touch $HOME/broken-test/lock_zone/dpkg_lock.lock
