id: lnx-inspect-installed-packages
title: "Query installed packages database"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "rm -rf $HOME/pkg-test"
    - "mkdir -p $HOME/pkg-test"
version: 1
