id: lnx-secure-mounted-storage
title: "Secure Mounted Storage Permissions"
xp: 30
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/storage-test/mounted_dir"
    - "chmod 700 $HOME/storage-test/mounted_dir"
version: 1
