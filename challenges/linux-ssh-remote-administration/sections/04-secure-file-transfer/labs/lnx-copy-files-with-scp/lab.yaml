id: lnx-copy-files-with-scp
title: "Copy Files Remotely using SCP"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/ssh-test"
    - "rm -f $HOME/ssh-test/transfer_cmd.sh"
version: 1
