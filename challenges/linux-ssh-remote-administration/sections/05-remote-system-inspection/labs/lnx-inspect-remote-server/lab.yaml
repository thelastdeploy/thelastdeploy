id: lnx-inspect-remote-server
title: "Inspect Remote Server Health and Generate Reports"
xp: 30
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/ssh-test"
    - "rm -f $HOME/ssh-test/audit_remote.sh"
    - "rm -f $HOME/ssh-test/audit_report.txt"
version: 1
