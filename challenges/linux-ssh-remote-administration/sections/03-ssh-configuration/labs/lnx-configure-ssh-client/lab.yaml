id: lnx-configure-ssh-client
title: Configure SSH Client Connection Aliases
xp: 25
setup:
  seed_commands:
  - mkdir -p $HOME/ssh-test/config_dir
  - rm -f $HOME/ssh-test/config_dir/config
version: 2
