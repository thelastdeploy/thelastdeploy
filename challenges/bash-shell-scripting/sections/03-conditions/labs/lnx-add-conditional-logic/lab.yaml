id: lnx-add-conditional-logic
title: "Add Conditional Logic"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/script-test"
    - "rm -f $HOME/script-test/check_num.sh"
version: 1
