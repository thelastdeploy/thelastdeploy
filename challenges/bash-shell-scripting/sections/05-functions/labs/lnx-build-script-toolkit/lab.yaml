id: lnx-build-script-toolkit
title: "Build a Script Helper Toolkit"
xp: 35
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/script-test"
    - "rm -f $HOME/script-test/toolkit.sh"
version: 1
