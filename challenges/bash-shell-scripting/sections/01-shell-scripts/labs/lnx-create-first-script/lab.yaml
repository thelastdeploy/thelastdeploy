id: lnx-create-first-script
title: "Create Your First Shell Script"
xp: 15
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/script-test"
    - "rm -f $HOME/script-test/hello.sh"
version: 1
