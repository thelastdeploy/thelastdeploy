id: nginx-permanent-redirect
title: "Configure Permanent Redirect"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.bak || true"
    - "sudo systemctl start nginx || true"
version: 1
