id: nginx-custom-404
title: "Configure Custom 404 Page"
xp: 25
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/default.bak || true"
    - "sudo rm -f /var/www/html/404.html"
    - "sudo systemctl start nginx || true"
version: 1
