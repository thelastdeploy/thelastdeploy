id: tf-list-functions
title: Use Built-in Collection Functions
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
  - rm -rf $HOME/terraform-expr-challenge
  - mkdir -p $HOME/terraform-expr-challenge
  - echo 'variable "users" { default = ["alice", "bob", "charlie"] }' > $HOME/terraform-expr-challenge/main.tf
version: 1
