id: tf-string-functions
title: Use Built-in String Functions
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
  - rm -rf $HOME/terraform-expr-challenge
  - mkdir -p $HOME/terraform-expr-challenge
  - echo 'variable "raw_text" { default = "  Hello World  " }' > $HOME/terraform-expr-challenge/main.tf
version: 1
