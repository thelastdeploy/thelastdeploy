id: lnx-schedule-system-maintenance
title: "Configure System-Wide Crontab Maintenance"
xp: 40
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/cron-test"
    - "rm -f $HOME/cron-test/sys_crontab"
version: 1
