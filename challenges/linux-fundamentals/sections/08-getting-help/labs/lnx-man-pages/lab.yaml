id: lnx-man-pages
title: "Look up options in man pages"
xp: 25
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/help-test"
    - "rm -f $HOME/help-test/parents_flag.txt"
version: 1
