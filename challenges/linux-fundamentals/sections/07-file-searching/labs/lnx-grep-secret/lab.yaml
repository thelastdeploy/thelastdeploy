id: lnx-grep-secret
title: "Search secret line using grep"
xp: 30
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/search-test/configs/prod"
    - "echo 'database_port=5432' > $HOME/search-test/configs/prod/app.conf"
    - "echo 'app_secret=devlab-key' >> $HOME/search-test/configs/prod/app.conf"
    - "rm -f $HOME/search-test/secret_line.txt"
version: 1
