id: lnx-copy-files
title: "Copy files in a directory"
xp: 25
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/files-test"
    - "echo 'copy-me' > $HOME/files-test/source.txt"
    - "rm -f $HOME/files-test/destination.txt"
version: 1
