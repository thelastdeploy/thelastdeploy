id: lnx-delete-files
title: "Delete files using rm"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/files-test"
    - "echo 'delete-me' > $HOME/files-test/delete-me.txt"
version: 1
