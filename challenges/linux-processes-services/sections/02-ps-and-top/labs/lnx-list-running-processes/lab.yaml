id: lnx-list-running-processes
title: "Record your running processes"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/process-test"
    - "rm -f $HOME/process-test/my_processes.txt"
version: 1
