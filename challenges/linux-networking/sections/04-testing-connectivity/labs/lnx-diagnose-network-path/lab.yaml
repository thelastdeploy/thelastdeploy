id: lnx-diagnose-network-path
title: "Trace network hops"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/network-test"
    - "rm -f $HOME/network-test/trace_cmd.txt"
version: 1
