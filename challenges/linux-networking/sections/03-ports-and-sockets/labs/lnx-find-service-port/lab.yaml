id: lnx-find-service-port
title: "Query the standard SSH service port"
xp: 15
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/network-test"
    - "rm -f $HOME/network-test/ssh_port.txt"
version: 1
