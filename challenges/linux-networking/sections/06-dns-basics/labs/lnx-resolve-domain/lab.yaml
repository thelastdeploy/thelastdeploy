id: lnx-resolve-domain
title: "Resolve a domain name local mapping"
xp: 15
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/network-test"
    - "rm -f $HOME/network-test/resolved_dns.txt"
version: 1
