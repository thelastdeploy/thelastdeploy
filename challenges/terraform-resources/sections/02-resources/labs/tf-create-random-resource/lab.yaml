id: tf-create-random-resource
title: Create a Random Resource
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
  - rm -rf $HOME/terraform-resources-challenge
  - mkdir -p $HOME/terraform-resources-challenge
  - echo 'provider "random" {}' > $HOME/terraform-resources-challenge/main.tf
version: 1
