id: lnx-modify-application-configuration
title: Modify Configuration Settings and Preserve Backups
xp: 30
setup:
  seed_commands:
  - mkdir -p $HOME/env-test
  - printf 'MAX_CONNECTIONS=100\nTIMEOUT=30\n' > $HOME/env-test/service.conf
  - rm -f $HOME/env-test/service.conf.bak
