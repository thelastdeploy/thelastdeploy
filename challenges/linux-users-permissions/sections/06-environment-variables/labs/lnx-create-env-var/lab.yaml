id: lnx-create-env-var
title: "Declare a local shell variable"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/env-test"
    - "rm -f $HOME/env-test/local_var.txt"
version: 1
