id: lnx-persistent-env-var
title: "Make an environment variable persistent"
xp: 25
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    # Cleanup any previous entries we added to bashrc
    - "sed -i '/PLATFORM_NAME/d' $HOME/.bashrc || true"
version: 1
