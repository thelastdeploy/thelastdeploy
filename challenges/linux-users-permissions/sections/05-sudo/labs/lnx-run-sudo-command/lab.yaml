id: lnx-run-sudo-command
title: "View protected credentials using sudo"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/sudo-test"
    - "rm -f $HOME/sudo-test/admin_only.txt"
version: 1
