id: lnx-identify-user
title: "Identify your active username"
xp: 20
estimated_minutes: 5
setup:
  type: shell
  seed_commands:
    - "mkdir -p $HOME/users-groups-test"
    - "rm -f $HOME/users-groups-test/current_user.txt"
version: 1
